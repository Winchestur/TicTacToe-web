<div>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="{{ path("/") }}">Tic Tac Toe</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                    aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path("/") }}">
                            {{ dict.home }} <span class="sr-only">(current)</span>
                        </a>
                    </li>
                    {% if hasRole('ROLE_USER') %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path("/user/profile") }}">{{ user.username }}</a>
                        </li>
                        {#<li class="nav-item dropdown">#}
                            {#<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"#}
                               {#data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">#}
                                {#{{ dict.menu }}#}
                            {#</a>#}
                            {#<div class="dropdown-menu" aria-labelledby="navbarDropdown">#}
                                {#{% if hasRole('ROLE_ADMIN') %}#}
                                    {#<a class="dropdown-item" href="{{ path("/admin/panel") }}">Admin panel</a>#}
                                    {#<a class="dropdown-item" href="{{ path("/admin/upload") }}">Upload</a>#}
                                {#{% endif %}#}
                            {#</div>#}
                        {#</li>#}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path("/logout") }}">{{ dict.logout }}</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path("/login") }}">{{ dict.login }}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path("/register") }}">{{ dict.register }}</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</div>
<!-- Used to fulfil the navbar space since the navbar is fixed. -->
<div style="height: 60px"></div>